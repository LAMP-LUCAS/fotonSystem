# Relatório de Handoff — 2026-07-06e

## 1. Conquistas da Sessão
- **STORY-041 concluída**: Encerramento de Dívidas Técnicas + Telemetria + NPS
- 4 sub-tarefas entregues (A-D) com 14/14 Acceptance Criteria cobertos
- **962 testes passam**, 3 falham (pré-existentes em `test_story016_ux_menu_restructuring.py`), zero regressão

## 2. Estado Atual
- **Story Ativa:** STORY-041 (status: completed)
- **Épico:** EPIC-004 (status: active)
- **Sprint:** 2026-SPRINT-9 (última story concluída)

## 3. Arquivos Criados/Alterados

| Arquivo | Ação | Propósito |
|---------|------|-----------|
| `foton_system/interfaces/mcp/foton_mcp.py` | Modificado | Removeu `_find_system_python()` + `import subprocess` (RAG-3.1); adicionou `duracao_ms` no retorno de `consultar_conhecimento` |
| `foton_system/core/ops/op_query_knowledge.py` | Modificado | Adicionou `[RAG_PERF]` log com query_hash, duration_ms, modelo, pipeline |
| `foton_system/core/ops/op_index_knowledge.py` | Modificado | Adicionou `linha_inicio`/`linha_fim` aos metadados dos chunks |
| `scripts/performance_baseline.py` | Modificado | Adicionou `--active`, `--mode` (minilm/bgem3/dual), `--queries` para carga ativa |
| `.opencode/templates/NPS_RAG.md` | **Novo** | Template de coleta NPS do módulo RAG |
| `docs/prd/epics/EPIC-004.md` | Modificado | `primeira_coleta_nps: 2026-07-06` adicionado ao histórico |
| `.opencode/backlog/stories/STORY-041-debt-telemetry-nps.md` | Modificado | Correção de arquivo alvo (op_query_knowledge → foton_mcp) + descritivo de metadados |
| `tests/unit/test_op_index_knowledge.py` | Modificado | +3 testes (RAG-2.3 metadata fields, RAG-2.3 linha monotonic, RAG-2.4 batch) |
| `tests/unit/test_rag_filters.py` | Modificado | +4 testes (RAG-5.2 score percentual, source_path, contexto markers, CLI format) |
| `tests/unit/test_mcp_server.py` | Modificado | Teste `test_empty_results_returns_message` atualizado para PT-BR + duracao_ms |

## 4. Detalhamento Técnico

### A — Subprocess Mode (RAG-3.1)
- `_find_system_python()` (linhas 62-73) e `import subprocess` (linha 20) removidos de `foton_mcp.py`
- Função não era chamada em nenhum lugar do código
- RAG-3.1 já estava ~~tachado~~ na SPEC-RAG-v2.0.md

### B — Metadados (linha_inicio/linha_fim)
- `op_index_knowledge.py`: no loop de chunks, calcula `linha_inicio`/`linha_fim` via `content[:pos].count('\n') + 1`
- Novo conjunto de metadados: `source`, `filename`, `hash`, `chunk_index`, `linha_inicio`, `linha_fim`
- Log mais rico para análise posterior de chunking quality

### C — Telemetria
- `[RAG_PERF] consulta=<hash> duration_ms=X modelo=Y pipeline=Z` logado via `logging.getLogger("op_query_knowledge")`
- `consultar_conhecimento` MCP retorna `⏱ duracao_ms=X` no final da string
- `performance_baseline.py --active --mode bgem3 --queries 10` executa carga ativa e gera baseline JSON

### D — NPS
- Pergunta: "Em uma escala de 0 a 10, o quanto o módulo de Consulta Inteligente (RAG) te ajuda a encontrar informações de projetos passados?"
- Template em `.opencode/templates/NPS_RAG.md` com instruções de coleta, cálculo e meta

### Rastreabilidade

| RULE-ID | Status |
|---------|--------|
| RAG-2.3 | ✔ Testes de metadados (source, filename, hash, chunk_index, linha_inicio, linha_fim) |
| RAG-2.4 | ✔ Teste de batch upsert (5+ chunks em uma chamada) |
| RAG-3.1 | ✔ Código removido, spec ~~tachado~~ |
| RAG-5.2 | ✔ Testes de formatação TUI (score %, fonte, contexto >>>...<<<) |

### Correções aplicadas ao plano original
1. `_find_system_python()` estava em `foton_mcp.py`, não em `op_query_knowledge.py` (story corrigido)
2. `BaseOp.execute()` já media `duration_ms` com `time.perf_counter()` (C1 já atendido)
3. `scripts/performance_baseline.py` já existia (precisou ser estendido, não criado)
4. Metadados expandidos com `linha_inicio`/`linha_fim` além do spec original

## 5. Próximos Passos
1. **Nova Sprint / Nova Story** — EPIC-004 concluído. Próximo épico da fila: EPIC-006 (Inteligência Financeira), EPIC-007 (Cronograma), EPIC-008 (Suprimentos), EPIC-009 (Conformidade), EPIC-010 (Diário de Obra), etc.

## 6. Contexto para Próximo Agente
STORY-041 foi a última story da **2026-SPRINT-9** e do **EPIC-004** (RAG Pipeline v2.0). O sistema de RAG agora possui: schema de configuração validado (STORY-040), telemetria inline com log `[RAG_PERF]` e baseline ativo, metadados enriquecidos com linha_inicio/fim, NPS estruturado, e código legado de subprocess removido. O épico EPIC-004 está completo com 12 stories (STORY-030 a STORY-041). A próxima sprint deve selecionar um novo épico do backlog (EPIC-006 em diante).
