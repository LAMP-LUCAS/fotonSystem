# Handoff — 2026-07-03

## Story Concluída: STORY-034 — TUI RAG Configuration

**Status:** ✅ Completo (197/197 testes passando)

## O que foi feito

Adicionado submenu **RAG** (opção 8) dentro do menu **Configurações** no TUI, com:

| Funcionalidade | Comando | AC |
|---|---|---|
| Diagnóstico de hardware + coleções ChromaDB | `display_rag_menu` → opção 1 | RULE-RAG-7.3 |
| Troca de modo de embedding (local/nuvem/híbrido) com validação de hardware | opção 2 | RULE-RAG-9.4 |
| Reindexação da base (full ou por cliente) | opção 3 | RULE-RAG-12.3 |
| Status dos modelos instalados | opção 4 | RULE-RAG-12.4 |

## Arquivos modificados/criados

- **NOVO:** `foton_system/interfaces/cli/menus_rag.py` — `MenuRagHandler` (~130 linhas)
- **MOD:** `foton_system/interfaces/cli/menus_config.py` — opção 8 no display + routing
- **MOD:** `foton_system/interfaces/cli/menus.py` — lazy registration do `MenuRagHandler`
- **NOVO:** `tests/unit/test_story034_tui_rag_config.py` — 16 testes (unitários + integração)

## Decisões de design

1. **Lazy import** do `MenuRagHandler` em `menus.py` para evitar falhas de ordem nos testes preexistentes (`test_story015`, `test_cli_menus_comprehensive`)
2. Padrão consistente com `MenuClientsHandler`, `MenuFinanceHandler`, etc.
3. `show_change_mode_menu` usa `HardwareProfiler.detect()` + `ModelRouter.validate_pipeline_feasibility()` inline para alertas visuais antes de trocar modo

## APIs core consumidas (já existentes de STORY-030..033)

- `HardwareProfiler` — detecção de HW (RAM, GPU, VRAM)
- `ModelRegistry` — lista/status de modelos
- `ModelRouter` — validação de pipeline por modo
- `DownloadManager` — download de modelo
- `VectorStoreManager` — estatísticas de coleções
- `OpIndexKnowledge` — reindexação

## Pendências futuras

- Nenhuma para esta story. Próximo passo natural: EPIC-004 (RAG) segue para features de query avançada ou integração com MCP.

## Testes

```bash
# Suite completa RAG + menus
pytest tests/unit/test_story034_tui_rag_config.py tests/unit/test_rag_filters.py tests/unit/test_story015_ux_menus_split_helpers.py tests/unit/test_cli_menus_comprehensive.py tests/unit/test_hardware_profiler.py tests/unit/test_model_registry.py tests/unit/test_model_router.py tests/unit/test_download_manager.py tests/unit/test_pipeline_nodes.py tests/unit/test_vector_store_manager.py -q
# → 197 passed

# Apenas STORY-034
pytest tests/unit/test_story034_tui_rag_config.py -v
# → 16 passed
```
